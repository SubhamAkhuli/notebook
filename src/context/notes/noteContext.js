import { createContext } from "react";


// createContext is a function that creates a context to be used in the app. It takes a default value as an argument.
const NoteContext = createContext();
export default NoteContext;